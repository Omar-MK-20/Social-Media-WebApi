# Port number on which the server application listens for incoming HTTP requests
SEVER_PORT = 

# MongoDB connection string (URI format string)
DB_URI =

# MongoDB fallback connection string (URI format string, only in production)
DB_URI_FALLBACK = 

# 64-character hexadecimal key (256-bit) used for symmetric encryption (e.g., AES-256)
ENCRYPTION_SECRET_KEY = 

# 32-character hexadecimal key (128-bit) used for signing admin JWT access tokens (HMAC secret)
TOKEN_SIGNATURE_ADMIN_ACCESS = 

# 32-character hexadecimal key (128-bit) used for signing user JWT access tokens (HMAC secret)
TOKEN_SIGNATURE_USER_ACCESS = 

# 32-character hexadecimal key (128-bit) used for signing admin JWT refresh tokens
TOKEN_SIGNATURE_ADMIN_REFRESH = 

# 32-character hexadecimal key (128-bit) used for signing user JWT refresh tokens
TOKEN_SIGNATURE_USER_REFRESH = 

# # Google OAuth Client ID (structured identifier issued by Google)
# GOOGLE_CLIENT_ID = 

# Gmail address used for SMTP authentication
GOOGLE_NODEMAILER_USER = 

# Google App Password (App-specific password generated from Google account)
GOOGLE_APP_PASSWORD = 

